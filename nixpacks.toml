# Railway Nixpacks configuration
# Installs system dependencies required for PDF processing

[phases.setup]
nixPkgs = ["poppler_utils"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"
